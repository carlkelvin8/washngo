import { AppError } from '@/lib/errors'; import { supabase } from '@/lib/supabase'; import type { Profile, VerificationStatus } from '@/types/domain';
export async function getAdminMetrics() { const { data, error } = await supabase.rpc('admin_dashboard_metrics'); if (error) throw new AppError('Unable to load platform metrics.', error); return data as Record<string, number>; }
export async function listVerificationRequests(): Promise<Profile[]> { const { data, error } = await supabase.from('profiles').select('*').in('role', ['rider','laundry_partner']).eq('status', 'pending').order('created_at'); if (error) throw new AppError('Unable to load verification requests.', error); return (data ?? []) as Profile[]; }
export async function setVerification(profileId: string, status: VerificationStatus) { const { error } = await supabase.rpc('set_account_status', { p_profile_id: profileId, p_status: status }); if (error) throw new AppError('Unable to update verification.', error); }
