import { useMutation, useQuery } from '@tanstack/react-query'; import { Text, View } from 'react-native'; import { AccountCard } from '@/components/account-card'; import { Badge, Button, Card, EmptyState, ErrorState, LoadingState, Metric, Screen, Title, ui } from '@/components/ui'; import { listMyOrders, transitionOrder } from '@/services/order.service'; import { queryClient } from '@/lib/query-client'; import type { Order, OrderStatus } from '@/types/domain';
const next: Partial<Record<OrderStatus, { label: string; status: OrderStatus }>> = { laundry_confirmation: { label: 'Accept booking', status: 'confirmed' }, received_by_laundry: { label: 'Start processing', status: 'processing' }, processing: { label: 'Ready for return', status: 'ready_for_return' } };
function OrderCard({ order }: { order: Order }) { const action = next[order.status]; const mutation = useMutation({ mutationFn: () => transitionOrder(order.id, action!.status), onSuccess: () => void queryClient.invalidateQueries({ queryKey: ['partner-orders'] }) }); return <Card><View style={ui.between}><Text style={ui.h2}>{order.order_number}</Text><Badge>{order.status.replaceAll('_',' ')}</Badge></View><Text style={ui.body}>Pickup {order.pickup_date} · {order.estimated_weight} kg estimated</Text><Text style={ui.price}>₱{order.total_amount.toFixed(2)}</Text>{action ? <Button loading={mutation.isPending} onPress={() => mutation.mutate()}>{action.label}</Button> : null}{order.status === 'laundry_confirmation' ? <Button variant="danger" onPress={() => void transitionOrder(order.id, 'rejected', 'Rejected by partner').then(() => queryClient.invalidateQueries({ queryKey: ['partner-orders'] }))}>Reject</Button> : null}</Card>; }
export default function LaundryDashboard() { const query = useQuery({ queryKey: ['partner-orders'], queryFn: listMyOrders }); if (query.isLoading) return <LoadingState />; if (query.isError) return <ErrorState message="Partner orders are unavailable." retry={() => void query.refetch()} />; const active = query.data?.filter((o) => !['completed','cancelled','rejected'].includes(o.status)) ?? []; const revenue = query.data?.filter((o) => o.status === 'completed').reduce((sum, o) => sum + o.laundry_subtotal, 0) ?? 0; return <Screen><Title eyebrow="Partner workspace">Laundry operations</Title><View style={ui.grid}><Metric label="Active orders" value={active.length} /><Metric label="Laundry revenue" value={`₱${revenue.toFixed(0)}`} /></View>{active.length ? active.map((o) => <OrderCard key={o.id} order={o} />) : <EmptyState title="All caught up" message="Incoming bookings will appear here in realtime." />}<AccountCard /></Screen>; }
