import { useState } from 'react'; import { Controller, useForm } from 'react-hook-form'; import { zodResolver } from '@hookform/resolvers/zod'; import { Link } from 'expo-router'; import { Text, View } from 'react-native'; import { Button, Card, Field, Screen, Title, ui } from '@/components/ui'; import { friendlyError } from '@/lib/errors'; import { signIn } from '@/services/auth.service'; import { loginSchema, type LoginValues } from '@/features/auth/schemas';
export default function LoginScreen() { const [error, setError] = useState(''); const { control, handleSubmit, formState: { errors, isSubmitting } } = useForm<LoginValues>({ resolver: zodResolver(loginSchema), defaultValues: { email: '', password: '' } }); const submit = handleSubmit(async (values) => { setError(''); try { await signIn(values.email, values.password); } catch (cause) { setError(friendlyError(cause)); } }); return <Screen><View style={{ paddingTop: 42 }}><Title eyebrow="Pick up · Clean · Deliver">Laundry day, handled.</Title><Text style={ui.body}>Book trusted Lipa City laundry partners and follow every handoff live.</Text></View><Card>{error ? <Text style={{ color: '#D64545' }}>{error}</Text> : null}<Controller control={control} name="email" render={({ field }) => <Field label="Email" autoCapitalize="none" keyboardType="email-address" value={field.value} onChangeText={field.onChange} error={errors.email?.message} />} /><Controller control={control} name="password" render={({ field }) => <Field label="Password" secureTextEntry value={field.value} onChangeText={field.onChange} error={errors.password?.message} />} /><Button onPress={submit} loading={isSubmitting}>Sign in</Button><Link href="/forgot-password" style={ui.link}>Forgot password?</Link></Card><Text style={ui.body}>New to WashNgo? <Link href="/register" style={ui.link}>Create a customer account</Link></Text></Screen>; }
