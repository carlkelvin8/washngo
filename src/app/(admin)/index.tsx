import { useMutation, useQuery } from '@tanstack/react-query'; import { Text, View } from 'react-native'; import { AccountCard } from '@/components/account-card'; import { Button, Card, ErrorState, LoadingState, Metric, Screen, Title, ui } from '@/components/ui'; import { getAdminMetrics, listVerificationRequests, setVerification } from '@/services/admin.service'; import { queryClient } from '@/lib/query-client';
export default function AdminDashboard() { const metrics = useQuery({ queryKey: ['admin-metrics'], queryFn: getAdminMetrics }); const requests = useQuery({ queryKey: ['verification-requests'], queryFn: listVerificationRequests }); const verify = useMutation({ mutationFn: ({ id, status }: { id: string; status: 'approved' | 'rejected' }) => setVerification(id, status), onSuccess: () => void queryClient.invalidateQueries({ queryKey: ['verification-requests'] }) }); if (metrics.isLoading || requests.isLoading) return <LoadingState />; if (metrics.isError || requests.isError) return <ErrorState message="Admin data could not be loaded." retry={() => { void metrics.refetch(); void requests.refetch(); }} />; const m = metrics.data ?? {}; return <Screen><Title eyebrow="Lipa City launch">WashNgo control center</Title><View style={ui.grid}><Metric label="Users" value={m.total_users ?? 0} /><Metric label="Active orders" value={m.active_orders ?? 0} /><Metric label="Orders today" value={m.orders_today ?? 0} /><Metric label="Platform revenue" value={`₱${Number(m.platform_revenue ?? 0).toFixed(0)}`} /></View><Text style={ui.h2}>Verification requests</Text>{requests.data?.map((profile) => <Card key={profile.id}><Text style={ui.h2}>{profile.full_name}</Text><Text style={ui.body}>{profile.role.replace('_',' ')} · joined {new Date(profile.created_at).toLocaleDateString()}</Text><View style={ui.row}><View style={ui.flex}><Button loading={verify.isPending} onPress={() => verify.mutate({ id: profile.id, status: 'approved' })}>Approve</Button></View><View style={ui.flex}><Button variant="danger" onPress={() => verify.mutate({ id: profile.id, status: 'rejected' })}>Reject</Button></View></View></Card>)}<AccountCard /></Screen>; }
