export class AppError extends Error { constructor(message: string, readonly cause?: unknown) { super(message); this.name = 'AppError'; } }
export function friendlyError(error: unknown): string { if (error instanceof AppError) return error.message; if (error instanceof Error && /network|fetch/i.test(error.message)) return 'You appear to be offline. Check your connection and try again.'; return 'Something went wrong. Please try again.'; }
export function assertData<T>(data: T | null, error: { message: string } | null, fallback: string): T { if (error) { console.error(fallback, error); throw new AppError(fallback, error); } if (data === null) throw new AppError(fallback); return data; }
