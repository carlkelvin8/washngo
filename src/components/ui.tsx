import type { PropsWithChildren, ReactNode } from 'react';
import { ActivityIndicator, Pressable, ScrollView, StyleSheet, Text, TextInput, type TextInputProps, useWindowDimensions, View } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';
import { colors, radius, space } from '@/constants/design';
export function Screen({ children, scroll = true }: PropsWithChildren<{ scroll?: boolean }>) { const { width } = useWindowDimensions(); const content = <View style={[styles.screenContent, width >= 760 && styles.screenContentWide]}>{children}</View>; return <SafeAreaView style={styles.safe}>{scroll ? <ScrollView contentContainerStyle={styles.scroll} keyboardShouldPersistTaps="handled">{content}</ScrollView> : content}</SafeAreaView>; }
export function Title({ children, eyebrow }: PropsWithChildren<{ eyebrow?: string }>) { return <View style={styles.titleWrap}>{eyebrow ? <Text style={styles.eyebrow}>{eyebrow}</Text> : null}<Text style={styles.title}>{children}</Text></View>; }
export function Card({ children, onPress }: PropsWithChildren<{ onPress?: () => void }>) { return <Pressable disabled={!onPress} onPress={onPress} style={({ pressed }) => [styles.card, pressed && styles.pressed]}>{children}</Pressable>; }
export function Button({ children, onPress, disabled, loading, variant = 'primary' }: PropsWithChildren<{ onPress?: () => void; disabled?: boolean; loading?: boolean; variant?: 'primary' | 'secondary' | 'danger' }>) { return <Pressable accessibilityRole="button" disabled={disabled || loading} onPress={onPress} style={({ pressed }) => [styles.button, variant === 'secondary' && styles.buttonSecondary, variant === 'danger' && styles.buttonDanger, (disabled || loading) && styles.disabled, pressed && styles.pressed]}>{loading ? <ActivityIndicator color={variant === 'secondary' ? colors.blue : colors.white} /> : <Text style={[styles.buttonText, variant === 'secondary' && styles.buttonSecondaryText]}>{children}</Text>}</Pressable>; }
export function Field({ label, error, ...props }: TextInputProps & { label: string; error?: string }) { return <View style={styles.field}><Text style={styles.label}>{label}</Text><TextInput placeholderTextColor={colors.muted} style={[styles.input, error && styles.inputError]} {...props} />{error ? <Text style={styles.error}>{error}</Text> : null}</View>; }
export function Badge({ children, tone = 'info' }: PropsWithChildren<{ tone?: 'info' | 'success' | 'warning' | 'danger' }>) { return <View style={[styles.badge, tone === 'success' && styles.success, tone === 'warning' && styles.warning, tone === 'danger' && styles.danger]}><Text style={styles.badgeText}>{children}</Text></View>; }
export function EmptyState({ title, message, action }: { title: string; message: string; action?: ReactNode }) { return <View style={styles.empty}><Text style={styles.emptyIcon}>◌</Text><Text style={styles.h2}>{title}</Text><Text style={styles.body}>{message}</Text>{action}</View>; }
export function LoadingState({ label = 'Loading…' }: { label?: string }) { return <View style={styles.center}><ActivityIndicator color={colors.blue} size="large" /><Text style={styles.body}>{label}</Text></View>; }
export function ErrorState({ message, retry }: { message: string; retry?: () => void }) { return <EmptyState title="We hit a snag" message={message} action={retry ? <Button onPress={retry}>Try again</Button> : undefined} />; }
export function Metric({ label, value, hint }: { label: string; value: string | number; hint?: string }) { return <View style={styles.metricCard}><Text style={styles.metric}>{value}</Text><Text style={styles.metricLabel}>{label}</Text>{hint ? <Text style={styles.caption}>{hint}</Text> : null}</View>; }
export const ui = StyleSheet.create({ row: { flexDirection: 'row', gap: space.md, alignItems: 'center' }, between: { flexDirection: 'row', justifyContent: 'space-between', gap: space.md, alignItems: 'center' }, grid: { flexDirection: 'row', flexWrap: 'wrap', gap: space.md }, flex: { flex: 1 }, h2: { fontSize: 18, lineHeight: 23, fontWeight: '900', color: colors.ink, letterSpacing: -0.35 }, body: { fontSize: 14, lineHeight: 21, color: colors.muted }, caption: { fontSize: 11, lineHeight: 16, color: colors.muted, fontWeight: '600' }, price: { fontSize: 19, color: colors.navy, fontWeight: '900', letterSpacing: -0.3 }, link: { color: colors.blue, fontWeight: '800' }, divider: { height: 1, backgroundColor: '#E8EDF5', marginVertical: space.md }, section: { gap: space.md, marginTop: space.xl } });
const styles = StyleSheet.create({ safe: { flex: 1, backgroundColor: colors.canvas }, scroll: { flexGrow: 1 }, screenContent: { flex: 1, width: '100%', maxWidth: 720, alignSelf: 'center', paddingHorizontal: 18, paddingTop: 18, paddingBottom: 36, gap: 14 }, screenContentWide: { paddingHorizontal: 28, paddingTop: 28, gap: 18 }, titleWrap: { gap: 5, marginBottom: 4 }, eyebrow: { color: colors.blue, fontWeight: '900', fontSize: 10, letterSpacing: 1.15, textTransform: 'uppercase' }, title: { color: colors.navy, fontSize: 28, lineHeight: 33, letterSpacing: -1, fontWeight: '900' }, card: { backgroundColor: colors.white, borderRadius: radius.lg, padding: 18, gap: 11, borderWidth: 1, borderColor: '#E8EEF7', shadowColor: colors.shadow, shadowOpacity: 0.065, shadowRadius: 18, shadowOffset: { width: 0, height: 8 } }, pressed: { opacity: 0.82, transform: [{ scale: 0.995 }] }, button: { minHeight: 54, borderRadius: radius.md, paddingHorizontal: space.lg, alignItems: 'center', justifyContent: 'center', backgroundColor: colors.blue, shadowColor: colors.blue, shadowOpacity: 0.18, shadowRadius: 9, shadowOffset: { width: 0, height: 5 } }, buttonSecondary: { backgroundColor: colors.white, borderWidth: 1.5, borderColor: '#BFD0ED', shadowOpacity: 0 }, buttonDanger: { backgroundColor: colors.red }, buttonText: { color: colors.white, fontSize: 15, fontWeight: '900' }, buttonSecondaryText: { color: colors.blue }, disabled: { opacity: 0.45 }, field: { gap: 7 }, label: { fontSize: 13, fontWeight: '800', color: colors.ink }, input: { minHeight: 54, paddingHorizontal: space.lg, borderWidth: 1.5, borderColor: colors.line, borderRadius: radius.md, backgroundColor: '#FBFCFF', color: colors.ink, fontSize: 15 }, inputError: { borderColor: colors.red }, error: { color: colors.red, fontSize: 12 }, badge: { alignSelf: 'flex-start', paddingHorizontal: 11, paddingVertical: 6, borderRadius: radius.pill, backgroundColor: colors.blueSoft }, success: { backgroundColor: colors.greenSoft }, warning: { backgroundColor: '#FFF5E6' }, danger: { backgroundColor: '#FFEBEB' }, badgeText: { fontSize: 10, color: colors.navy, fontWeight: '900', textTransform: 'capitalize', letterSpacing: .2 }, empty: { flex: 1, minHeight: 260, alignItems: 'center', justifyContent: 'center', gap: space.md, padding: space.xl }, emptyIcon: { width: 64, height: 64, borderRadius: 22, backgroundColor: colors.blueSoft, textAlign: 'center', paddingTop: 8, fontSize: 34, color: colors.blue }, h2: { fontSize: 20, fontWeight: '900', color: colors.ink, textAlign: 'center' }, body: { maxWidth: 340, fontSize: 14, lineHeight: 21, color: colors.muted, textAlign: 'center' }, center: { flex: 1, minHeight: 300, justifyContent: 'center', alignItems: 'center', gap: space.md, backgroundColor: colors.canvas }, metricCard: { minWidth: 145, flexGrow: 1, flexBasis: '45%', backgroundColor: colors.white, padding: 17, borderRadius: 20, borderWidth: 1, borderColor: '#E8EEF7' }, metric: { fontSize: 26, fontWeight: '900', color: colors.navy, letterSpacing: -.8 }, metricLabel: { marginTop: 3, fontSize: 12, fontWeight: '800', color: colors.muted }, caption: { color: colors.muted, fontSize: 11 } });
