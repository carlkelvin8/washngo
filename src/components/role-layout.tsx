import type { PropsWithChildren } from 'react'; import { Redirect, Stack } from 'expo-router'; import type { AppRole } from '@/types/domain'; import { useAuthStore } from '@/store/auth.store';
export function RoleLayout({ role }: PropsWithChildren<{ role: AppRole }>) { const { session, profile } = useAuthStore(); if (!session) return <Redirect href="/login" />; if (profile && profile.role !== role) return <Redirect href="/" />; return <Stack screenOptions={{ headerShadowVisible: false, headerBackTitle: 'Back', headerTintColor: '#2166F3', headerStyle: { backgroundColor: '#F5F8FF' }, headerTitleStyle: { color: '#11233F', fontWeight: '900' }, contentStyle: { backgroundColor: '#F5F8FF' } }}><Stack.Screen name="index" options={{ headerShown: false }} /></Stack>; }
