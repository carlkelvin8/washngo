import type { PropsWithChildren } from 'react'; import { Redirect, Stack } from 'expo-router'; import type { AppRole } from '@/types/domain'; import { useAuthStore } from '@/store/auth.store';
export function RoleLayout({ role }: PropsWithChildren<{ role: AppRole }>) { const { session, profile } = useAuthStore(); if (!session) return <Redirect href="/login" />; if (profile && profile.role !== role) return <Redirect href="/" />; return <Stack screenOptions={{ headerShadowVisible: false, headerTitleStyle: { fontWeight: '800' } }} />; }
