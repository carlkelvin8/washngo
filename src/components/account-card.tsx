import { Text } from 'react-native'; import { Button, Card, ui } from '@/components/ui'; import { signOut } from '@/services/auth.service'; import { useAuthStore } from '@/store/auth.store';
export function AccountCard() { const profile = useAuthStore((s) => s.profile); return <Card><Text style={ui.h2}>{profile?.full_name}</Text><Text style={ui.body}>{profile?.role.replace('_',' ')} · {profile?.status}</Text><Button variant="secondary" onPress={() => void signOut()}>Sign out</Button></Card>; }
