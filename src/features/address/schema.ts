import { z } from 'zod'; export const addressSchema = z.object({ label: z.string().min(2), full_address: z.string().min(8), barangay: z.string().min(2), latitude: z.coerce.number().min(-90).max(90), longitude: z.coerce.number().min(-180).max(180) });
