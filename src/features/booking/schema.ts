import { z } from 'zod'; export const bookingSchema = z.object({ addressId: z.string().uuid('Choose an address.'), pickupDate: z.string().regex(/^\d{4}-\d{2}-\d{2}$/, 'Use YYYY-MM-DD.'), pickupTime: z.string().regex(/^\d{2}:\d{2}$/, 'Use HH:MM.'), estimatedWeight: z.coerce.number().min(1).max(50), specialInstructions: z.string().max(500), paymentMethod: z.enum(['cod','pay_later']) }); export type BookingValues = z.infer<typeof bookingSchema>;
